# Simple Model

### Model Architecture:

* Conv (3, 16)
* ReLU
* MaxPool2d

* Conv (16, 32)
* ReLU
* MaxPool2d

* Flatten
* Linear (32 * 32 * 32, 128)
* ReLU
* Linear (128, 5)

### Learning

Not my best one

- Static: Model architecture and dataset
- Applied Modifications: Data Augmentation, hyperparameter tuning, Adding class weight due to class imbalance in dataset
- Best Accuracy: 58%
- Limitations: Architecture is too small. The classifier is big and the sequential layer is small, causing no major feature learning happening and giving classifier too much load.
